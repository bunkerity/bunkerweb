# Example-backed spec: the stack under test is examples/redmine itself,
# deployed the way a user would deploy it. The assertions live here so that the
# example directory holds only what someone reading the documentation needs.
example: redmine

integrations:
  - "Docker"
  - "Autoconf"

actions:
  string_check:
    type: string
    url: "https://www.example.com"
    verify_ssl: false
    string: "redmine"
    ignore_case: true
    follow_redirects: true
    timeout: 120
    delay: 180
