{% if USE_WHITELIST == "yes" +%}
    {% if WHITELIST_IP != "" +%}
SecRule REMOTE_ADDR "@ipMatch {{ WHITELIST_IP.replace(" ", ",") }}" "id:1000,phase:1,t:none,nolog,pass,ctl:ruleEngine=Off"
    {% endif +%}
{% endif +%}