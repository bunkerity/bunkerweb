{
  "id": "ui",
  "name": "UI",
  "description": "Integrate easily the BunkerWeb UI.",
  "version": "1.0",
  "stream": "no",
  "settings": {
    "USE_UI": {
      "context": "multisite",
      "default": "no",
      "help": "Use UI",
      "id": "use-ui",
      "label": "Use UI",
      "regex": "^(yes|no)$",
      "type": "check"
    }
  }
}
