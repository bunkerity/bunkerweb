{
  "id": "ssl",
  "name": "SSL",
  "description": "Handle SSL/TLS related settings.",
  "version": "1.0",
  "stream": "yes",
  "settings": {
    "REDIRECT_HTTP_TO_HTTPS": {
      "context": "multisite",
      "default": "no",
      "help": "Redirect all HTTP request to HTTPS.",
      "id": "redirect-http-to-https",
      "label": "Redirect HTTP to HTTPS",
      "regex": "^(yes|no)$",
      "type": "check"
    },
    "AUTO_REDIRECT_HTTP_TO_HTTPS": {
      "context": "multisite",
      "default": "yes",
      "help": "Try to detect if HTTPS is used and activate HTTP to HTTPS redirection if that's the case.",
      "id": "auto-redirect-http-to-https",
      "label": "Auto redirect HTTP to HTTPS",
      "regex": "^(yes|no)$",
      "type": "check"
    },
    "SSL_PROTOCOLS": {
      "context": "multisite",
      "default": "TLSv1.2 TLSv1.3",
      "help": "The supported version of TLS. We recommend the default value TLSv1.2 TLSv1.3 for compatibility reasons.",
      "id": "ssl-protocols",
      "label": "SSL protocols",
      "regex": "^(?! )( ?(?:SSLv[23]|TLSv1(?:\\.[1-3])?))+$",
      "type": "multiselect",
      "multiselect": [
        {
          "id": "SSLv3",
          "label": "SSLv3 (deprecated)",
          "value": "SSLv3"
        },
        {
          "id": "TLSv1",
          "label": "TLSv1 (1.0)",
          "value": "TLSv1"
        },
        {
          "id": "TLSv1.1",
          "label": "TLSv1.1",
          "value": "TLSv1.1"
        },
        {
          "id": "TLSv1.2",
          "label": "TLSv1.2 (recommended)",
          "value": "TLSv1.2"
        },
        {
          "id": "TLSv1.3",
          "label": "TLSv1.3 (recommended)",
          "value": "TLSv1.3"
        }
      ]
    },
    "SSL_CIPHERS_LEVEL": {
      "context": "multisite",
      "default": "modern",
      "help": "Preset security level for SSL cipher suites. 'Modern' is most secure but may not work with older devices.",
      "id": "ssl-ciphers-level",
      "label": "SSL Ciphers Level",
      "regex": "^(modern|intermediate|old)$",
      "type": "select",
      "select": ["modern", "intermediate", "old"]
    },
    "SSL_CIPHERS_CUSTOM": {
      "context": "multisite",
      "default": "",
      "help": "Custom SSL cipher suite string. If specified, overrides the SSL Ciphers Level. Leave empty to use level-based configuration.",
      "id": "ssl-ciphers-custom",
      "label": "Custom SSL Ciphers",
      "regex": "^(?!:)(:?[A-Za-z0-9-]+)*$",
      "type": "text"
    },
    "SSL_ECDH_CURVE": {
      "context": "multisite",
      "default": "auto",
      "help": "Preferred ECDH curves (TLS groups) for key exchange. Use \"auto\" for smart selection (prefers PQC groups like X25519MLKEM768 on OpenSSL 3.5+ when available).",
      "id": "ssl-ecdh-curve",
      "label": "SSL ECDH Curves",
      "regex": "^[A-Za-z0-9:_-]+$",
      "type": "text"
    },
    "SSL_SESSION_CACHE_SIZE": {
      "context": "multisite",
      "default": "10m",
      "help": "Size of the SSL session cache (e.g., '10m' for 10 megabytes, '512k' for 512 kilobytes). Set to 'off' or 'none' to disable session caching.",
      "id": "ssl-session-cache-size",
      "label": "SSL Session Cache Size",
      "regex": "^(off|none|[1-9][0-9]*[kKmMgG]?)$",
      "type": "text"
    }
  }
}
