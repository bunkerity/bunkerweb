{
  "id": "gzip",
  "name": "Gzip",
  "description": "Compress HTTP requests with the gzip algorithm.",
  "version": "1.0",
  "stream": "no",
  "settings": {
    "USE_GZIP": {
      "context": "multisite",
      "default": "no",
      "help": "Enable or disable Gzip compression.",
      "id": "use-gzip",
      "label": "Use gzip",
      "regex": "^(yes|no)$",
      "type": "check"
    },
    "GZIP_TYPES": {
      "context": "multisite",
      "default": "application/atom+xml application/javascript application/json application/rss+xml application/vnd.ms-fontobject application/x-font-opentype application/x-font-truetype application/x-font-ttf application/x-javascript application/xhtml+xml application/xml font/eot font/opentype font/otf font/truetype image/svg+xml image/vnd.microsoft.icon image/x-icon image/x-win-bitmap text/css text/javascript text/plain text/xml",
      "help": "List of MIME types that will be compressed with gzip.",
      "id": "gzip-types",
      "label": "MIME types",
      "regex": "^( *[\\-\\w.]+/[\\-\\w.+]+ *)+$",
      "type": "multivalue"
    },
    "GZIP_MIN_LENGTH": {
      "context": "multisite",
      "default": "1000",
      "help": "Minimum response size (in bytes) for Gzip compression to apply.",
      "id": "gzip-min-length",
      "label": "Minimum length",
      "regex": "^\\d+$",
      "type": "number"
    },
    "GZIP_COMP_LEVEL": {
      "context": "multisite",
      "default": "5",
      "help": "Compression level for Gzip (1 = least compression, 9 = maximum compression).",
      "id": "gzip-comp-level",
      "label": "Compression level",
      "regex": "^[1-9]$",
      "type": "select",
      "select": ["1", "2", "3", "4", "5", "6", "7", "8", "9"]
    },
    "GZIP_PROXIED": {
      "context": "multisite",
      "default": "no-cache no-store private expired auth",
      "help": "Specifies which proxied requests should be compressed.",
      "id": "gzip-proxied",
      "label": "Proxied requests",
      "regex": "^.*$",
      "type": "multiselect",
      "multiselect": [
        {
          "id": "off",
          "label": "off - Disable for all proxied requests",
          "value": "off"
        },
        {
          "id": "expired",
          "label": "expired - Expires header is present",
          "value": "expired"
        },
        {
          "id": "no-cache",
          "label": "no-cache - Cache-Control: no-cache",
          "value": "no-cache"
        },
        {
          "id": "no-store",
          "label": "no-store - Cache-Control: no-store",
          "value": "no-store"
        },
        {
          "id": "private",
          "label": "private - Cache-Control: private",
          "value": "private"
        },
        {
          "id": "no_last_modified",
          "label": "no_last_modified - No Last-Modified header",
          "value": "no_last_modified"
        },
        {
          "id": "no_etag",
          "label": "no_etag - No ETag header",
          "value": "no_etag"
        },
        {
          "id": "auth",
          "label": "auth - Authorization header is present",
          "value": "auth"
        },
        {
          "id": "any",
          "label": "any - Enable for all proxied requests",
          "value": "any"
        }
      ]
    }
  }
}
